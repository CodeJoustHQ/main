package com.rocketden.main.dto.room;

import lombok.Getter;
import lombok.Setter;

@Getter
@Setter
public class CreateRoomRequest {
    // Empty for now, but in the future this could have things like:
    // 1. User initiating the request
    // 2. game settings
}
